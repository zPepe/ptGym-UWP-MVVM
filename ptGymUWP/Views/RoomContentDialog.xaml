<ContentDialog
    x:Class="ptGymUWP.Views.RoomContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ptGymUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Sala"
    PrimaryButtonText="OK"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <StackPanel>
        <TextBox Header="Capacidade" Text="{x:Bind RoomViewModel.Room.Capacity, Mode=TwoWay}"/>
        <TextBox Header="Nome" Text="{x:Bind RoomViewModel.Room.Name, Mode=TwoWay}"/>
    </StackPanel>
</ContentDialog>
